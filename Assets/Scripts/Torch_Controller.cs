using UnityEngine;
using UnityEngine.Rendering.Universal;

public class Torch_Controller : MonoBehaviour
{
    // TODO
    /*
    public Light2D light;

    private float _target;

    void Start()
    {
        light = GetComponent<Light2D>();
        _target = Random.Range(0.3f, 0.8f);
    }

    void Update()
    {
        light.falloffIntensity = Mathf.Lerp(light.intensity, _target, Time.deltaTime * 15);

        if(Mathf.Abs(light.intensity - _target) < 0.01f)
        {
            _target = Random.Range(0.3f, 0.8f);
        }
    }
    */
}
